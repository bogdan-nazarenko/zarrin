const Section = () => {
    return (
        <section>
            <div className="container">
                <h1>React + Vite + SCSS</h1>
            </div>
        </section>
    );
};

export default Section;
